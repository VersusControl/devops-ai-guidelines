# Generated by:
#   helm template k8s-mcp ../../helm/k8s-mcp-server > rendered.yaml
#
# Commit the rendered manifest so the kustomize overlays can target it for
# GitOps-style review. Regenerate before each release.
apiVersion: v1
kind: ConfigMap
metadata:
  name: k8s-mcp-placeholder
data:
  note: "Run 'make render' (in chapter 09) and overwrite this file with the helm output."
